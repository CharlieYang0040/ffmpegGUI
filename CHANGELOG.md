# 변경 기록

## 2.0.2 — 2026-08-03

### 타임라인 동기화와 재정렬

- VFR 영상은 고정 FPS 환산 대신 FFprobe의 실제 프레임 PTS로 탐색·재생
  위치와 타임코드를 계산
- 트림 적용 직후 재생 위치를 선택 구간 안으로 맞추고, 재생이 Out 지점을
  넘으면 해당 프레임에서 정지하도록 보정
- 재생 종료 후 타임라인을 탐색할 때 Windows Media Foundation이 마지막
  프레임을 유지하던 상태를 다시 디코딩해 요청 프레임으로 갱신
- 클립 본문을 앞·뒤·마지막 위치로 끌어 놓을 수 있도록 삽입 위치 표시와
  드롭 좌표 계산 보강
- 목록 재정렬을 Qt 모델의 행 이동으로 바꿔 기존 미디어 위젯이 지연 삭제되며
  `NoneType.clip_id` 예외가 발생하던 원인 제거

### 작업 공간

- 저장된 분할 비율이 비정상적으로 넓어도 출력 패널을 300~380px 범위로
  정규화하고 남은 폭을 미리보기에 배정
- 자동 회귀 기준선을 101개 테스트로 확대

## 2.0.1 — 2026-07-30

### 타임라인과 미리보기

- 트림 시 목록 전체를 재생성하던 구조를 클립 범위 단일 갱신으로 교체
- 원본 전체 범위와 실제 사용 구간을 분리해 좌우 핸들 위치를 안정적으로 표시
- 분할·복제·재정렬·삭제·목록 복구에서 기존 미디어 분석 결과와 위젯 재사용
- I/O, Home/End, 한 프레임 이동과 실행 취소·다시 실행의 구간 좌표 통합
- 미디어 정보 로딩 전 MP4를 이미지 시퀀스로 처리하던 초기화 순서 수정
- 미리보기 표시 초기화가 저장된 편집 구간을 덮어쓰지 않도록 상태 경계 분리

### 미디어와 출력 안정성

- 모든 ffprobe/FFmpeg 보조 프로세스의 콘솔 창 숨김 처리 통합
- MKV·MOV·WebM 등 목표 컨테이너에 맞춘 remux와 원자적 최종 파일 교체
- 출력 충돌 시 번호 붙이기·덮어쓰기·취소 선택과 원본 파일 보호
- QVideoSink 기반 영상 프레임 표시와 비동기 미디어 작업 수명 정리

### 검증

- 자동 테스트 92개
- CFR, VFR, PNG 시퀀스, WebP, 취소, 재실행, NVENC 실제 회귀 7/7 통과
- 실제 메인 창에서 연속 좌우 트림, 복제, 분할, 이동과 실행 취소 검증
- Windows x64 단일 실행 파일 빌드와 실제 메인 창 시작·종료 검증

## 2.0.0 — 2026-07-30

### 새 기능

- 소스, 미리보기, 결과 설정과 하단 단일 트랙을 중심으로 UI 전면 개편
- 타임라인 직접 선택·탐색·트림·분할·복제·삭제·재정렬
- 타임라인 확대·축소, 가로 이동, 비동기 썸네일 반영과 화면 밖 렌더링 생략
- 목적 중심 CPU/NVENC 프리셋과 실행 전 문제 해결 안내
- 비모달 진행 상태와 완료 결과 다시 열기·재실행
- 설정과 고급 기능을 기본 작업 공간에서 분리

### 안정성

- `WorkspaceState`와 `ClipRange`로 목록·타임라인·인코딩 구간 통합
- VFR, 이미지 시퀀스, 애니메이션 WebP 트림 보정
- 실제 초기화 probe로 NVENC 드라이버·런타임 호환성 확인
- 취소 시 자식 FFmpeg·작업 스레드·진행 UI 정리 및 즉시 재실행 지원
- 비동기 메타데이터 로딩 중 개별 트림 손실 방지
- OTIO의 제거된 FFmpeg 경로 참조 복구

### 개발과 배포

- Python 3.13 및 PyInstaller 6.21 기준으로 갱신
- Windows CI와 태그 기반 GitHub Release 워크플로 추가
- 재현 가능한 릴리즈 빌드와 SHA-256 체크섬 생성
- MIT 라이선스 파일과 현재 구조에 맞는 한국어 문서 정비

### 검증

- 자동 테스트 74개
- 실제 미디어 회귀 7개: CFR, VFR, PNG 시퀀스, WebP, 취소, 재실행, NVENC
- 1366×768에서 표시 배율 100%, 125%, 150%, 200% 레이아웃 확인
- Windows x64 단일 실행 파일 빌드와 시작·종료 스모크 확인

### 알려진 제한

- Windows 전용, 코드 서명 없음
- 실제 출력 장치의 주관적 오디오 청취는 제외하고 오디오 스트림·비무음
  신호·앱 재생 진행으로 검증
